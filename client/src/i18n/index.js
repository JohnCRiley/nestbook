// Internationalisation — placeholder for EN / FR / ES / DE translations.
// A library like i18next will be wired up here when we build the UI.

export const SUPPORTED_LOCALES = ['en', 'fr', 'es', 'de'];
export const DEFAULT_LOCALE = 'en';
